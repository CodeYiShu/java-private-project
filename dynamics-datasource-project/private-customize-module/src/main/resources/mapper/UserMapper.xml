<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.codeshu.mapper.UserMapper">
    <select id="test01" resultType="com.codeshu.entity.User">
        select id, username, password
        from sys_user
    </select>

    <select id="test02" resultType="com.codeshu.entity.User">
        select id, username, password
        from sys_user
    </select>

    <select id="test03" resultType="com.codeshu.entity.User">
        select id, username, password
        from sys_user
    </select>
</mapper>
